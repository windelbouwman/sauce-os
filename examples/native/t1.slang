
pub fn add(a: int, b: int) -> int:
	"""Simplest function possible"""
	#let x = 2025 + a
	# a + b + x + 13
	#x + 2
	a + b + 42

pub fn sub(a: int, b: int) -> int:
	"""Simplest function possible"""
	let x = 2025 + a
	a + b - x + 13
	# x + 2
	# a + b - 2
