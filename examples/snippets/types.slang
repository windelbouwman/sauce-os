
# Example with some basic types
from std import print

fn main() -> int:
    let a = 1
    let b = 2.0
    let c = "drie"
    # let d = u8(2)
    # let d = u16(2)
    # let d = u32(2)
    print("a = {a}")
    print("c = {c}")
    return 0
